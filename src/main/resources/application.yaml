spring:
  application:
    name: task-monitor

  datasource:
    url: jdbc:postgresql://localhost:5432/task_monitor
    username: postgres
    password: pg@ryanDev

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect